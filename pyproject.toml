[tool.poetry]
name = "afrojogos-app"
version = "0.1.0"
description = ""
authors = ["André <andre.aslima@hotmail.com>"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
django = "^5.2.1"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
